<% if model.errors.any? %>
  <div class="error text-danger">
    <h2><%= model.errors.count %>件のエラーがあります。</h2>
    <ul>
      <% model.errors.full_messages.each do |message| %>
      <li class="text-danger"><%= message %></li>
      <% end %>
    </ul>
  </div>
 <% end %>